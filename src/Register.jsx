const Register= () => {
    return(
        <div>
            <h1>Register</h1>
            <button onClick={() => window.location.href = 'login'}>go to login</button>
        </div>
    )
}

export default Register;